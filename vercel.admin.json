{
  "framework": "vite",
  "installCommand": "npm ci",
  "buildCommand": "npm run build:admin",
  "outputDirectory": "dist-admin",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
